[build-system]
requires = ["setuptools>=69.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mcp-app-playground"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["mcp-app-suite>=0.1.0"]

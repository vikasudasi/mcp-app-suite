# {{ app_display_name }}

Generated with `mcp-app-scaffolder --template node --simple`.

## Run server

```bash
npm install
npm run start
```

This variant embeds the HTML app directly in `server.js` (no Vite project).
